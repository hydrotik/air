{"batch":"Cross-cutting Sweep","batch_index":7,"assessments":{"api_surface_coherence":72.0,"design_coherence":78.0},"dimension_notes":{"api_surface_coherence":{"evidence":["`src/dashboard/hooks/useTelemetry.ts` exposes a large public fetch surface where almost every method returns raw `res.json()` data with no shared response type, while server routes in `src/server/index.ts` return a mix of arrays, objects, and ad hoc fallback shapes.","`src/db/queries.ts` aliases backend fields into snake_case result sets (`server_name`, `avg_ms`, `tokens_used`, etc.), and dashboard panels such as `src/dashboard/visualizations/McpStatsPanel.tsx` and `src/dashboard/visualizations/RagStatsPanel.tsx` re-declare those SQL-shaped DTOs locally instead of consuming a stable contract.","`src/sdk/client.ts` overloads semantic fields in emitted events: `ratePrompt()` writes `this.provider` into the `model` field, and auto-latency from MCP/RAG traces stores `serverName`/`source` in `model`, while `src/shared/events.ts` and aggregations in `src/db/queries.ts` treat `model` as an actual model dimension."],"impact_scope":"subsystem","fix_scope":"multi_file_refactor","confidence":"high"},"design_coherence":{"evidence":["`src/dashboard/dashboard.css.ts` establishes shared layout and tokenized styles, but visualization files like `src/dashboard/visualizations/McpStatsPanel.tsx`, `src/dashboard/visualizations/RagStatsPanel.tsx`, `src/dashboard/visualizations/ActivitySparklines.tsx`, and `src/dashboard/visualizations/ContextUtilizationChart.tsx` bypass that layer with hardcoded inline fonts, spacing, and colors.","The dashboard mixes three UI construction patterns for similar panels: bespoke inline-table renderers (`McpStatsPanel`, `RagStatsPanel`), direct SVG/inline-style widgets (`ActivitySparklines`, `ContextTreemap`), and a design-system `DataGrid` (`DriftPanel`) without a shared visualization shell or row primitive.","`src/dashboard/hooks/useTelemetry.ts` also centralizes websocket state, polling, and more than a dozen endpoint-specific helpers in one hook, which pushes orchestration complexity outward and encourages each panel to define its own local data/view conventions."],"impact_scope":"subsystem","fix_scope":"multi_file_refactor","confidence":"medium"}},"findings":[{"dimension":"api_surface_coherence","identifier":"dashboard_api_contract_fragmentation","summary":"The dashboard consumes SQL-shaped JSON directly, so the public API contract is split across queries, routes, and individual panels instead of being defined once.","related_files":["src/dashboard/hooks/useTelemetry.ts","src/db/queries.ts","src/server/index.ts","src/dashboard/visualizations/McpStatsPanel.tsx","src/dashboard/visualizations/RagStatsPanel.tsx"],"evidence":["`useTelemetry()` returns raw `await res.json()` for nearly every endpoint and does not publish typed response models for the rest of the dashboard to rely on.","`TelemetryStore` query methods expose backend column aliases such as `server_name`, `tool_name`, `avg_ms`, `tokens_used`, and `context_window`, and the panels re-declare matching local interfaces instead of importing a shared DTO.","The `/api/sessions/:id/context/latest` route returns `{ ...row, breakdown: [...] }`, while the hook's fallback returns `{ segments: [] }`, showing that even one endpoint's shape is already ambiguous at the boundary."],"suggestion":"Introduce a shared dashboard API contract layer: define response DTOs in one module, map DB rows to those DTOs inside `src/server/index.ts` or a dedicated presenter, and have `useTelemetry()` return typed results instead of raw JSON.","confidence":"high","impact_scope":"subsystem","fix_scope":"multi_file_refactor","root_cause_cluster":"missing_shared_api_contracts"},{"dimension":"api_surface_coherence","identifier":"silent_empty_fallbacks_hide_request_failures","summary":"Dashboard fetch helpers collapse transport errors into empty data, so callers cannot distinguish 'no telemetry yet' from 'API failed'.","related_files":["src/dashboard/hooks/useTelemetry.ts","src/server/index.ts","src/dashboard/visualizations/McpStatsPanel.tsx","src/dashboard/visualizations/RagStatsPanel.tsx"],"evidence":["In `useTelemetry.ts`, methods such as `fetchSessionEvents`, `fetchToolStats`, `fetchMcpStats`, `fetchRagStats`, `fetchDriftEvents`, and many others `catch` and return `[]`; `fetchLatestBreakdown` returns `{ segments: [] }` on failure.","The corresponding routes in `src/server/index.ts` legitimately return empty arrays or empty objects for valid states, so the fallback values are indistinguishable from a healthy-but-empty response.","Panels like `McpStatsPanel` and `RagStatsPanel` render 'No ... recorded' for empty arrays, which means backend outages are rendered as lack of activity instead of an error state."],"suggestion":"Change the hook helpers to return an explicit result shape such as `{ ok: true, data } | { ok: false, error }`, or at minimum surface a per-request error flag so empty telemetry and failed requests remain distinguishable.","confidence":"high","impact_scope":"subsystem","fix_scope":"multi_file_refactor","root_cause_cluster":"missing_shared_api_contracts"},{"dimension":"api_surface_coherence","identifier":"sdk_semantic_fields_are_overloaded","summary":"The SDK writes unrelated identifiers into `model`, which corrupts downstream prompt and latency aggregations that assume `model` really means model.","related_files":["src/sdk/client.ts","src/shared/events.ts","src/db/queries.ts","src/sdk/rag.ts","src/sdk/mcp.ts"],"evidence":["`ratePrompt()` in `src/sdk/client.ts` emits `model: this.provider`, even though `PromptRatingEvent` in `src/shared/events.ts` defines `model` and `provider` as separate semantic fields.","`traceMcp()` and `traceRetrieval()` auto-record latency with `recordLatency('api_call', durationMs, { model: serverName })` and `{ model: source }`, so non-model identifiers are stored in the latency model column.","`getPromptVariantComparison()` and `getLatencyStats()` in `src/db/queries.ts` group and label results by `model`, so provider names, MCP server names, and RAG source names become indistinguishable from actual LLM models in the reported API surface."],"suggestion":"Stop overloading `model` for non-model concepts. Add explicit fields for source/system identity in the emitted event payloads or make the latency/prompt helpers require real `model` and `provider` inputs before populating those columns.","confidence":"high","impact_scope":"subsystem","fix_scope":"multi_file_refactor","root_cause_cluster":"semantic_field_overloading"},{"dimension":"api_surface_coherence","identifier":"tracer_failure_contracts_diverge","summary":"Peer tracer methods in the SDK have incompatible success and failure telemetry behavior, so the tracing API is not predictable to consumers.","related_files":["src/sdk/client.ts","src/sdk/rag.ts","src/sdk/index.ts"],"evidence":["`traceMcp()` and `traceRetrieval()` both catch failures, emit error telemetry, and rethrow.","`traceEmbedding()` and `traceIndex()` directly await `fn()` and emit only on success, so failed embedding/index operations disappear from telemetry entirely.","`createRagTracer()` in `src/sdk/rag.ts` exposes `traceRetrieval`, `traceEmbedding`, and `traceIndex` as same-level API methods, but they do not share a consistent contract for emitted events or failure observability."],"suggestion":"Normalize all trace helpers to the same contract: emit start/end or success/error telemetry for every traced operation, and document whether automatic latency events are included. If one operation type intentionally differs, expose it under a distinct API name.","confidence":"high","impact_scope":"module","fix_scope":"multi_file_refactor","root_cause_cluster":"semantic_field_overloading"},{"dimension":"design_coherence","identifier":"dashboard_visualizations_form_style_islands","summary":"The dashboard’s visualization layer uses incompatible styling and composition patterns, so presentation changes require many one-off edits instead of flowing through shared primitives.","related_files":["src/dashboard/dashboard.css.ts","src/dashboard/visualizations/McpStatsPanel.tsx","src/dashboard/visualizations/RagStatsPanel.tsx","src/dashboard/visualizations/ActivitySparklines.tsx","src/dashboard/visualizations/ContextUtilizationChart.tsx","src/dashboard/visualizations/DriftPanel.tsx"],"evidence":["`src/dashboard/dashboard.css.ts` defines shared shell, panel, header, body, and event-feed styles backed by tokens, but the visualization components hardcode their own `fontFamily`, `rgba(...)` palette, spacing, and borders inline.","`McpStatsPanel` and `RagStatsPanel` hand-roll nearly identical table layouts independently instead of sharing a common stats-table primitive.","`DriftPanel` uses the design-system `DataGrid`, while `ActivitySparklines` and `ContextUtilizationChart` use bespoke inline-rendered widgets with unrelated color and typography conventions, creating multiple structural patterns for the same dashboard area."],"suggestion":"Create a small shared visualization kit for this dashboard: token-backed typography/color helpers plus a reusable stats-table/panel shell. Then migrate the existing panels onto those primitives so visual changes and accessibility fixes land once.","confidence":"medium","impact_scope":"subsystem","fix_scope":"multi_file_refactor","root_cause_cluster":"dashboard_presentation_fragmentation"}],"retrospective":{"root_causes":["No single shared contract defines the dashboard API boundary; DB row shapes, route payloads, hook return values, and panel-local interfaces all act as competing sources of truth.","The SDK relies on generic event helpers without preserving the semantics of fields like `model` and `provider`, which leaks misleading data into downstream analytics.","The dashboard UI lacks a small shared visualization abstraction layer, so each panel chose its own rendering and styling conventions."],"likely_symptoms":["dashboard_api_contract_fragmentation","silent_empty_fallbacks_hide_request_failures","sdk_semantic_fields_are_overloaded","tracer_failure_contracts_diverge","dashboard_visualizations_form_style_islands"],"possible_false_positives":[]}}