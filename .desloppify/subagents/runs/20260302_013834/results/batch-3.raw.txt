{"batch":"Abstractions & Dependencies","batch_index":3,"assessments":{"abstraction_fitness":72.0,"mid_level_elegance":69.0,"low_level_elegance":78.0},"dimension_notes":{"abstraction_fitness":{"evidence":["[src/sdk/rag.ts] hides a filesystem read behind `createRagTracersFromConfig()`, so a factory-looking SDK helper depends on `process.cwd()` and `.air.json` discovery through [src/shared/config.ts] instead of explicit inputs.","[src/server/cli.ts] and [src/server/start.ts] are parallel wrappers over [src/server/index.ts], but only the CLI path applies config loading, host selection, and redaction setup; the second wrapper adds no new abstraction value and exposes a divergent boot contract."],"impact_scope":"subsystem","fix_scope":"multi_file_refactor","confidence":"high","sub_axes":{"abstraction_leverage":68.0,"indirection_cost":74.0,"interface_honesty":65.0}},"mid_level_elegance":{"evidence":["[src/server/index.ts] owns ingestion, provider discovery, static file resolution, websocket handlers, REST routes, and RAG shim endpoints in one function, so nearly every integration seam is edited in one place.","[src/db/queries.ts] returns raw SQL row shapes such as `current_val`, `deviation_pct`, and `session_id`, and [src/dashboard/visualizations/DriftPanel.tsx] mirrors those names directly in UI-only interfaces instead of consuming a stable API DTO."],"impact_scope":"subsystem","fix_scope":"architectural_change","confidence":"high"},"low_level_elegance":{"evidence":["[src/shared/drift.ts] pushes the new metric value into the rolling window before computing baseline/deviation, so each candidate alert is measured against a baseline that already includes the spike.","[src/shared/drift.test.ts] validates drift severities against that contaminated baseline, which locks in the current behavior instead of protecting the intended threshold semantics."],"impact_scope":"module","fix_scope":"multi_file_refactor","confidence":"high"}},"findings":[{"dimension":"abstraction_fitness","identifier":"hidden_config_io_factory","summary":"The SDK's config-driven RAG factory hides ambient filesystem I/O behind a helper that reads like a pure constructor.","related_files":["src/sdk/rag.ts","src/shared/config.ts","src/server/index.ts","src/server/cli.ts"],"evidence":["`createRagTracersFromConfig()` in `src/sdk/rag.ts` unconditionally calls `loadConfig()`, which walks parent directories from `process.cwd()` and logs to the console from `src/shared/config.ts`.","The same config-loading policy is also embedded in the server boot path (`src/server/index.ts` and `src/server/cli.ts`), so config discovery is duplicated across runtime boundaries instead of being an explicit dependency passed in once."],"suggestion":"Split config discovery from object construction: keep `loadConfig()` as an explicit server/CLI concern, and make the SDK factory accept `RagProviderConfig[]` or an `AirConfig` object directly. If a convenience loader is kept, name it as an I/O helper and isolate its logging so callers can opt into that behavior.","confidence":"high","impact_scope":"subsystem","fix_scope":"multi_file_refactor","root_cause_cluster":"runtime_boundary_smearing"},{"dimension":"abstraction_fitness","identifier":"duplicate_server_entrypoints","summary":"Two server boot wrappers expose different startup contracts around the same `createServer()` core.","related_files":["src/server/start.ts","src/server/cli.ts","src/server/index.ts","src/shared/config.ts"],"evidence":["`src/server/start.ts` hard-codes port `5200`, host `0.0.0.0`, and bypasses config/redaction handling entirely.","`src/server/cli.ts` performs config loading, argument parsing, redaction selection, and richer startup reporting before calling the same `createServer()` function, so the extra wrapper in `start.ts` is a divergent duplicate rather than a meaningful abstraction."],"suggestion":"Collapse startup into one supported entrypoint or extract a shared boot helper that both wrappers call with the same options model. If `start.ts` only exists for local dev, make it delegate to the CLI boot path instead of re-implementing defaults.","confidence":"high","impact_scope":"module","fix_scope":"multi_file_refactor","root_cause_cluster":"runtime_boundary_smearing"},{"dimension":"mid_level_elegance","identifier":"drift_api_leaks_storage_shape","summary":"Drift data crosses the DB-to-UI seam as raw SQL column names, coupling the dashboard directly to table layout.","related_files":["src/db/queries.ts","src/dashboard/visualizations/DriftPanel.tsx","src/server/index.ts"],"evidence":["`getDriftEvents()` and `getDriftSummary()` in `src/db/queries.ts` return `SELECT *`/snake_case rows such as `current_val`, `deviation_pct`, `window_size`, and `session_id`.","`src/dashboard/visualizations/DriftPanel.tsx` re-declares those DB-shaped interfaces locally and renders them directly, so any schema rename in storage becomes a frontend change even though the server already owns the API boundary in `src/server/index.ts`."],"suggestion":"Introduce a drift DTO at the query or server layer with camelCase field names and domain semantics, and have the dashboard consume that mapped shape. That keeps SQL naming localized to storage concerns and gives the API room to evolve independently of SQLite schema details.","confidence":"high","impact_scope":"subsystem","fix_scope":"multi_file_refactor","root_cause_cluster":"runtime_boundary_smearing"},{"dimension":"mid_level_elegance","identifier":"server_index_is_integration_hub","summary":"`createServer()` concentrates unrelated orchestration concerns into one integration hub with no internal seams.","related_files":["src/server/index.ts","src/server/cli.ts","src/collector/index.ts"],"evidence":["`src/server/index.ts` owns provider registry state, event ingestion, drift emission, static dashboard discovery, websocket endpoints, session APIs, provider APIs, RAG shim APIs, and health/config reporting in a single function.","Because that orchestration is centralized, changes in collector semantics (`src/collector/index.ts`) or boot/config behavior (`src/server/cli.ts`) both force edits into the same server file instead of isolated route or service modules."],"suggestion":"Extract the server into explicit collaborators: an ingestion service, provider registry, route modules, and transport adapters for websocket/HTTP. Keep `createServer()` as composition only so each boundary can evolve without expanding a single hub file.","confidence":"high","impact_scope":"subsystem","fix_scope":"architectural_change","root_cause_cluster":"runtime_boundary_smearing"},{"dimension":"low_level_elegance","identifier":"drift_baseline_uses_current_sample","summary":"The drift detector mutates its rolling window before evaluating drift, which dilutes every spike against itself.","related_files":["src/shared/drift.ts","src/shared/drift.test.ts","src/server/index.ts"],"evidence":["In `src/shared/drift.ts`, `check()` pushes `value` into `window.values` and updates `window.sum` before computing `baseline` and `deviationPct`, so the candidate event is included in the baseline it is being judged against.","`src/shared/drift.test.ts` asserts severities produced by that diluted baseline instead of asserting against the prior window, so the current tests reinforce the misleading threshold behavior that `src/server/index.ts` relies on for live alerts."],"suggestion":"Compute drift against the pre-existing window, then append the new sample after classification. Update the tests to build a baseline window first and assert severities against the untouched baseline so threshold meanings remain stable.","confidence":"high","impact_scope":"module","fix_scope":"multi_file_refactor"}],"retrospective":{"root_causes":["Runtime boundaries are being treated as convenience layers, so config discovery, schema details, and boot policy leak across server, SDK, and UI.","The server package lacks smaller composition units, which encourages new features to be added as branches inside existing hub modules."],"likely_symptoms":["duplicate_server_entrypoints","drift_api_leaks_storage_shape"],"possible_false_positives":[]}}