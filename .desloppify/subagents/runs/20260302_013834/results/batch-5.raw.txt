{"batch":"AI Debt & Migrations","batch_index":5,"assessments":{"ai_generated_debt":81.0,"incomplete_migration":78.0,"low_level_elegance":82.0},"dimension_notes":{"ai_generated_debt":{"evidence":["`src/sdk/mcp.ts` builds two public MCP helper surfaces (`instrumentMcp`, `createMcpTracer`) that mostly forward directly into `AirClient.traceMcp` with minimal translation.","`src/sdk/rag.ts` repeats the same pattern for retrieval, embedding, index, generic trace, emit, and close, so the public API is largely a hand-written forwarding shell over `AirClient`.","Both files carry long example-heavy docblocks around very small forwarding bodies, which increases maintenance surface faster than behavior."],"impact_scope":"subsystem","fix_scope":"multi_file_refactor","confidence":"high"},"incomplete_migration":{"evidence":["`src/sdk/rag.ts:80-95` advertises config-derived embedding defaults but still requires `model: string`, so `options?.defaultEmbeddingModel` only helps if callers bypass the declared contract with `undefined`.","`src/shared/config.ts:79-84` still supports both `.air.json` and `air.config.json`, while `src/sdk/rag.ts:130-152` documents only `.air.json`, leaving the active config convention unresolved.","The RAG API is partly shifted toward config-driven defaults (`createRagTracersFromConfig`, provider `embeddingModel`/`dimensions`) but the call signature still reflects the older fully-manual style."],"impact_scope":"module","fix_scope":"multi_file_refactor","confidence":"high"},"low_level_elegance":{"evidence":["`src/shared/config.ts:86-99` mixes discovery, parsing, console logging, warning emission, and silent fallback to `{}` inside one low-level helper, so each library call to `loadConfig()` carries hidden I/O and user-facing side effects.","`src/sdk/rag.ts:155-170` calls `loadConfig()` inside a convenience factory, so constructing tracers from config also prints to stdout and hides parse failures behind an empty map.","`src/sdk/rag.ts:88` contains a dead-looking fallback chain (`model ?? options?.defaultEmbeddingModel ?? 'unknown'`) because `model` is declared as required."],"impact_scope":"module","fix_scope":"multi_file_refactor","confidence":"high"}},"findings":[{"dimension":"ai_generated_debt","identifier":"thin_wrapper_tracer_surface","summary":"MCP and RAG helpers are mostly pass-through shells over AirClient","related_files":["src/sdk/mcp.ts","src/sdk/rag.ts"],"evidence":["`createMcpTracer()` returns `traceToolCall`, `traceResourceRead`, `tracePromptGet`, `emit`, and `close`, each forwarding almost directly to `AirClient` with only fixed string literals.","`createRagTracer()` does the same for `traceRetrieval`, `traceEmbedding`, `traceIndex`, `trace`, `emit`, and `close`, so most of the file is wrapper ceremony rather than domain logic.","The duplicated wrapper style across both files points to a missing shared pattern for provider-specific tracing helpers rather than two independently justified APIs."],"suggestion":"Collapse these hand-written forwarding objects into one smaller abstraction: either expose `AirClient` directly with provider-bound factory helpers, or extract a shared builder that declares only the genuinely different method mappings per subsystem.","confidence":"high","impact_scope":"subsystem","fix_scope":"multi_file_refactor","root_cause_cluster":"provider_bound_wrapper_ceremony"},{"dimension":"incomplete_migration","identifier":"rag_default_model_migration_stalled","summary":"Config-driven embedding defaults are wired in but unusable through the declared API","related_files":["src/sdk/rag.ts","src/shared/config.ts"],"evidence":["`createRagTracersFromConfig()` copies `provider.embeddingModel` into `defaultEmbeddingModel`, implying callers should be able to omit the model when using config-backed tracers.","`traceEmbedding()` still requires `model: string`, so normal TypeScript callers must always provide a model and never benefit from the config default.","The fallback expression `model ?? options?.defaultEmbeddingModel ?? 'unknown'` shows an unfinished migration from explicit model passing to config-derived defaults."],"suggestion":"Finish the migration in one direction: either make `traceEmbedding(model, ...)` honestly require a model and remove `defaultEmbeddingModel`, or change the signature to allow omission and resolve the model from config before tracing.","confidence":"high","impact_scope":"module","fix_scope":"multi_file_refactor","root_cause_cluster":"partial_config_driven_migration"},{"dimension":"incomplete_migration","identifier":"dual_config_filename_shim_has_no_end_state","summary":"Two config filename conventions remain active without a clear canonical endpoint","related_files":["src/shared/config.ts","src/sdk/rag.ts"],"evidence":["`CONFIG_FILENAMES` in `src/shared/config.ts` searches both `.air.json` and `air.config.json`.","`src/sdk/rag.ts` examples and prose describe `.air.json` as the config source, not the two-file compatibility mode.","Keeping both names live without deprecation or precedence guidance leaves migration debt in the runtime contract and documentation."],"suggestion":"Pick one canonical filename, document it consistently, and treat the other as an explicitly deprecated compatibility path with a warning and planned removal date, or remove the shim if external support is no longer required.","confidence":"medium","impact_scope":"module","fix_scope":"multi_file_refactor","root_cause_cluster":"partial_config_driven_migration"},{"dimension":"low_level_elegance","identifier":"config_loader_has_hidden_console_side_effects","summary":"Low-level config loading performs unsolicited logging and silent fallback on every read","related_files":["src/shared/config.ts","src/sdk/rag.ts"],"evidence":["`loadConfig()` logs successful loads with `console.log` and parse failures with `console.warn`, even though it is a shared library helper rather than a CLI entrypoint.","On parse failure it returns `{}` after warning, so callers like `createRagTracersFromConfig()` quietly degrade to an empty tracer map instead of letting the caller choose how to handle bad config.","Because `createRagTracersFromConfig()` calls `loadConfig()` internally, simply constructing SDK helpers can emit stdout noise and hide broken configuration."],"suggestion":"Keep `loadConfig()` side-effect free: return `{ config, path }` or throw a typed error, and move all user-facing logging/warning decisions to CLI/server entrypoints that can choose the right behavior.","confidence":"high","impact_scope":"module","fix_scope":"multi_file_refactor","root_cause_cluster":"side_effectful_config_access"}],"retrospective":{"root_causes":["Provider-specific SDK surfaces were added as convenience APIs without enough unique behavior to justify their own hand-maintained wrapper layers.","The config system appears mid-transition toward config-driven defaults, but the public call signatures and file discovery contract were not completed together.","Low-level helpers are taking on presentation concerns (console output, silent degradation) instead of staying composable."],"likely_symptoms":["thin_wrapper_tracer_surface","rag_default_model_migration_stalled","dual_config_filename_shim_has_no_end_state","config_loader_has_hidden_console_side_effects"],"possible_false_positives":[]}}