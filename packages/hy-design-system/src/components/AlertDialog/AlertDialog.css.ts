import {
  baseOverlay,
  baseModalContent,
  baseModalHeader,
  baseModalFooter,
  baseModalTitle,
  baseModalDescription,
} from '../../styles/overlay.css';

/**
 * AlertDialog — shadcn v4 aligned.
 * Uses shared overlay/modal styles. Same as Dialog but without close button.
 */
export const alertDialogOverlay = baseOverlay;
export const alertDialogContent = baseModalContent;
export const alertDialogHeader = baseModalHeader;
export const alertDialogFooter = baseModalFooter;
export const alertDialogTitle = baseModalTitle;
export const alertDialogDescription = baseModalDescription;
