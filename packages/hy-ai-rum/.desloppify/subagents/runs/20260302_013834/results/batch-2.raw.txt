{"batch":"Conventions & Errors","batch_index":2,"assessments":{"convention_outlier":88.0,"error_consistency":72.0,"mid_level_elegance":78.0},"dimension_notes":{"convention_outlier":{"evidence":["`src/dashboard/hooks/useTelemetry.ts` mixes several naming families for nearly identical fetch helpers (`fetchToolStats`, `fetchProviderSummary`, `fetchCostBreakdown`, `fetchPromptVariants`) and then re-exports `fetchSessions` under a different verb as `refreshSessions` at lines 277-307.","`src/dashboard/dashboard.css.ts` keeps a consistent exported-style naming scheme for selectors, while `src/server/start.ts` stays minimal and conventional; the main drift in this batch is concentrated in the hook's API naming rather than across all three files."],"impact_scope":"module","fix_scope":"multi_file_refactor","confidence":"medium","issues_preventing_higher_score":"The hook API has naming drift that makes sibling operations harder to predict: some methods describe shape (`Stats`, `Timeseries`), some describe presentation (`Breakdown`, `Summary`), and one renames the same operation as `refreshSessions`."},"error_consistency":{"evidence":["`src/dashboard/hooks/useTelemetry.ts` suppresses WebSocket parse failures entirely with `catch { // ignore }` at lines 25-36, so malformed dashboard messages disappear without any signal.","The same hook converts every REST failure into success-shaped fallback values (`[]` or `{ segments: [] }`) across lines 51-261 and never checks `res.ok`, so HTTP errors, JSON parse errors, and true empty datasets become indistinguishable.","`src/server/start.ts` takes the opposite approach at process startup by logging the original error and exiting non-zero at lines 14-16, which makes failure handling within this batch inconsistent across adjacent boundaries."],"impact_scope":"subsystem","fix_scope":"multi_file_refactor","confidence":"high"},"mid_level_elegance":{"evidence":["`src/dashboard/hooks/useTelemetry.ts` exports a broad endpoint-mirroring surface of per-resource callbacks at lines 277-307 instead of exposing a smaller session-centric data boundary.","The hook also leaks transport normalization to callers: it returns raw JSON for most endpoints, but special-cases only `fetchLatestBreakdown` with a different fallback shape at lines 113-120, indicating boundary translation is ad hoc rather than centralized.","`src/server/start.ts` is a thin entrypoint with one clear responsibility, which highlights how much dashboard integration logic has accumulated into the hook interface instead of being hidden behind a cohesive client boundary."],"impact_scope":"subsystem","fix_scope":"multi_file_refactor","confidence":"high"}},"findings":[{"dimension":"error_consistency","identifier":"silent_dashboard_transport_failures","summary":"The dashboard hook swallows transport and parsing failures, turning backend faults into empty-but-valid data.","related_files":["src/dashboard/hooks/useTelemetry.ts","src/server/start.ts"],"evidence":["`useTelemetry` ignores malformed WebSocket payloads with a bare `catch` in `ws.onmessage` (lines 24-36).","Every REST helper in `useTelemetry` catches broadly and returns default values like `[]` or `{ segments: [] }` without checking `res.ok` (for example lines 51-75, 77-147, 151-261).","`start.ts` logs startup failures and exits at lines 14-16, so the server boundary is fail-fast while the client boundary is fail-silent for the same subsystem."],"suggestion":"Introduce a shared dashboard request helper that checks `res.ok`, preserves the original error, and returns a typed `{ data, error }` or raises a domain-specific error. Keep fallback defaults only at the final rendering boundary, and surface WebSocket parse failures through logging or explicit connection/error state.","confidence":"high","impact_scope":"subsystem","fix_scope":"multi_file_refactor","root_cause_cluster":"missing_dashboard_error_policy"},{"dimension":"mid_level_elegance","identifier":"endpoint_mirror_hook_surface","summary":"`useTelemetry` mirrors backend endpoints one function at a time, forcing consumers to orchestrate integration details instead of consuming a cohesive dashboard data boundary.","related_files":["src/dashboard/hooks/useTelemetry.ts","src/server/start.ts"],"evidence":["`useTelemetry` returns more than a dozen narrowly scoped fetch callbacks (`fetchToolCalls`, `fetchLatencyStats`, `fetchEvalTimeseries`, `fetchDriftSummary`, etc.) at lines 277-307.","The same hook mixes stateful concerns (`events`, `sessions`, `connected`) with raw endpoint wrappers and one-off fallback shaping (`fetchLatestBreakdown` returns `{ segments: [] }`, others return raw JSON) at lines 6-120 and 277-307.","`start.ts` stays as a single-purpose process entrypoint, so the seam complexity in this batch is concentrated in the dashboard hook rather than being distributed across clear collaborators."],"suggestion":"Refactor the hook around a smaller client boundary, for example a typed dashboard service or session-scoped query object that groups related session data and performs response normalization in one place. Expose higher-level operations such as `loadSessionDashboard(sessionId)` and `refreshDerivedViews(sessionId, eventTypes)` rather than endpoint-by-endpoint fetch wrappers.","confidence":"high","impact_scope":"subsystem","fix_scope":"multi_file_refactor","root_cause_cluster":"missing_dashboard_data_boundary"}],"retrospective":{"root_causes":["The dashboard client lacks a single boundary for transport, normalization, and error policy, so `useTelemetry` has become both connection manager and ad hoc API client.","Failure handling conventions were not standardized across process startup, WebSocket ingestion, and REST fetches."],"likely_symptoms":["silent_dashboard_transport_failures","endpoint_mirror_hook_surface"],"possible_false_positives":[]}}