import React from 'react';
import { ShowcaseGrid } from '@hydrotik/design-system';

/**
 * Showcase page — renders the shared ShowcaseGrid component.
 * The same grid is used in Storybook's Introduction page.
 */
export function ShowcasePage() {
  return <ShowcaseGrid />;
}
